# Optional ambient audio

Add a calm loop (MP3/OGG) such as `ambient-cozy.mp3` and wire it in `components/game/AmbientAudio.tsx` by setting `src` when you are ready.

The MVP uses **procedural UI sounds** (no external files required). Toggle sounds in Settings.
